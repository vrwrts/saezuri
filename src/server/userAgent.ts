// Wikimedia's User-Agent policy *requires* a descriptive agent naming the client
// and a reachable contact, and answers generic or absent agents with 403 — so this
// is load-bearing, not decoration. Keep the contact URL real.
export const USER_AGENT = 'Saezuri/0.1 (https://github.com/vrwrts/saezuri)'
